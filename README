# Init
git clone git@github.com:tnantoka/dots.git
mv dots/.* ~/
rm -rf dots

# Install neocomplcache
cd ~/.vim
git clone git://github.com/Shougo/neocomplcache.git
mv neocomplcache/* .
rm -rf neocomplcache

